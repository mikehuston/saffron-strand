%h1 Pending Events
- @events.each do |e|
	- if e.user
		%h3= "Event Type:" 
		=link_to "#{e.event_type}", admin_event_path(e.id)
		%h5= "Head Count: #{e.head_count}"
		%h5= "User Name: #{e.user.name}"
		%h5= "User E-mail: #{e.user.email}"